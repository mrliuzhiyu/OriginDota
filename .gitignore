# Dota 2 录像文件（太大，不入 git）
*.dem
*.dem.bz2

# 解析原始事件流（每场 15-20MB，太大不入 git，可重生成）
parsed/**/*.jsonl

# 注：parsed/**/*-summary.json 与 *-lane.json 是 AI 友好聚合数据（每场 ~15KB），
# 入 git 保留供后续深度分析。原始 dem 录像不在仓库时仍可基于这两个文件做战报。

# tools 临时下载产物
tools/_*.json

# tools 运行时输出（每次脚本重新生成）
tools/deepdive-output.json
tools/deepdive-report.md

# Node / Python
node_modules/
__pycache__/
*.pyc
.env
.env.local

# OS
Thumbs.db
.DS_Store

# 编辑器
.vscode/
.idea/
*.swp
